// TESTING (2026-07): Makes the @testing-library/jest-dom custom matchers
// (toBeInTheDocument, etc.) visible to TypeScript in the unit tests. The
// matchers themselves are registered at runtime in jest-setup.js.
/// <reference types="@testing-library/jest-dom" />
